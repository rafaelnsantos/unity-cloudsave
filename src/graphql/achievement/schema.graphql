type AchievementDone {
    title: String
    description: String
    completed: String
}

type Query {
    Achievements: [AchievementDone]
}

type Mutation {
    CompleteAchievement(title: String!): Boolean
}